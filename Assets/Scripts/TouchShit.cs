using UnityEngine;

public static class InputSimulator
{
    public static void KeyPress(KeyCode key)
    {
        // Simulate key press
        switch (key)
        {
            case KeyCode.Space:
                // Do whatever you want to simulate space bar press
                break;
            // You can add cases for other keys if needed
        }
    }
}
